<%= form_with(model: account, local: true) do |form| %>
  <% if account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
      <% account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount, id: :account_amount, :required => 'require' %>
  </div>

  <div class="field">
    <%= form.label :credit %>
    <%= form.select :credit, ['Credit', 'Debit'], {prompt: 'select...'}, id: :account_credit %>
  </div>

  <div class="field">
    <%= form.label :balance %>
 <!--   <%= form.text_field :balance, id: :account_balance %> -->
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
